# Secrets
kubectl apply -f secrets.yml

# Services
kubectl apply -f services.yml

# Security/Role
kubectl apply -f security.yml

# Configs
kubectl create configmap MAKEFILE_SERVICE_NAME-config --from-file=config.json
kubectl create configmap MAKEFILE_SERVICE_NAME-caddyfile --from-file=Caddyfile

# PVC
# Caddy config?
# Postgres

# Deploy
kubectl apply -f rabbitmq.yml
kubectl apply -f postgres.yml
kubectl apply -f caddy.yml
kubectl apply -f main.yml
#kubectl apply -f spawner.yml
