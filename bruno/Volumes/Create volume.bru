meta {
  name: Create volume
  type: http
  seq: 3
}

post {
  url: {{base_url}}/pods/volumes
  body: json
  auth: none
}

headers {
  X-Tapis-Token: {{X-Tapis-Token}}
  Content-Type: application/json
}

body:json {
  {
    "volume_id": "{{volume_id}}",
    "description": "My data volume"
  }
}
