meta {
  name: Upload to volume
  type: http
  seq: 13
}

post {
  url: {{base_url}}/pods/volumes/{{volume_id}}/upload/filename.txt
  body: multipartForm
  auth: none
}

headers {
  Accept: */*
  X-Tapis-Token: {{X-Tapis-Token}}
}

body:multipart-form {
  file: @file()
}
