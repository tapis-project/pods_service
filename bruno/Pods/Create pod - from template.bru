meta {
  name: pods: from template
  type: http
  seq: 4
}

post {
  url: {{base_url}}/pods
  body: json
  auth: none
}

headers {
  X-Tapis-Token: {{X-Tapis-Token}}
  Content-Type: application/json
}

body:json {
  {
    "pod_id": "{{pod_id}}",
    "template": "jupyterlab:latest"
  }
}
