# Tapis
tapipy>=1.8.1
tapisservice>=1.7.0
jsonschema==4.17.3 # Errors after this version with error below.
# # Error reading local "notifications" resource. Ensure path is absolute /usr/local/lib/python3.10/site-packages/tapipy/resources/openapi_v3-notifications.yml. e:cannot import name '_legacy_validators' from 'jsonschema' (/usr/local/lib/python3.10/site-packages/jsonschema/__init__.py)
openapi_spec_validator==0.5.4

# Service API
pydantic==2.11.4
fastapi==0.115.12
#fastapi==0.103.2
python-multipart
sqlmodel==0.0.24
uvicorn

# Databases/APIs
zstandard # node telemetry ingest Content-Encoding: zstd (gzip is the stdlib fallback)
sqlalchemy==2.0.41
alembic==1.15.2
psycopg2==2.9.9
kubernetes==28.1.0
neo4j-driver
rabbitpy
channelpy
python-multipart
pika
bcrypt # gate access-token password hashing (already present transitively via paramiko)

# Misc
pylint

# Dev
jupyterlab==4.1

# Tests
pytest
httpx # Required for testing, imported by: from fastapi.testclient import TestClient
paramiko