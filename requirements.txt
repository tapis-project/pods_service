# Tapis
tapipy>=1.8.1
tapisservice>=1.7.0
jsonschema==4.17.3 # Errors after this version with error below.
# # Error reading local "notifications" resource. Ensure path is absolute /usr/local/lib/python3.10/site-packages/tapipy/resources/openapi_v3-notifications.yml. e:cannot import name '_legacy_validators' from 'jsonschema' (/usr/local/lib/python3.10/site-packages/jsonschema/__init__.py)
openapi_spec_validator==0.5.4

# Service API
pydantic==2.11.4
fastapi==0.141.1 # no starlette upper bound from here on — starlette resolves to its 1.3.x line
python-multipart
sqlmodel==0.0.24
uvicorn

# Imported directly by service code. Both were previously satisfied only as
# transitive deps of jupyterlab — moving dev tools to their own venv exposed
# that (health.py died with ModuleNotFoundError: jinja2).
jinja2      # traefik/config template rendering (kubernetes_utils, volume_utils)
pyyaml      # api_misc, traefik config, spec handling

# Databases/APIs
zstandard # node telemetry ingest Content-Encoding: zstd (gzip is the stdlib fallback)
sqlalchemy==2.0.41
alembic==1.15.2
psycopg2==2.9.12 # 2.9.9 cannot build on python 3.13+; 2.9.12 builds on 3.10 and 3.13 both
kubernetes==36.0.3 # 28.x capped urllib3<2, which dragged tapipy back to 1.9.0 — this pin was the whole CVE chain
neo4j-driver
rabbitpy
channelpy
python-multipart
pika
bcrypt # gate access-token password hashing (already present transitively via paramiko)

# Dev-only tooling (jupyterlab, pylint) lives in requirements-dev.txt — installed
# only in the devtools image stage (docker build --target devtools), never in the
# runtime image. jupyterlab alone carried 27 pip-audit CVE advisories in here.

# Tests
pytest
httpx # Required for testing, imported by: from fastapi.testclient import TestClient
paramiko